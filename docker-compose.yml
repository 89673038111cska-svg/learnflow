services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: learnflow
      POSTGRES_PASSWORD: learnflow_dev_password
      POSTGRES_DB: learnflow
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U learnflow"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql+asyncpg://learnflow:learnflow_dev_password@postgres:5432/learnflow
      SECRET_KEY: ${SECRET_KEY:-dev_secret_key_change_in_production}
      MCP_API_TOKEN: ${MCP_API_TOKEN:-dev_mcp_token_change_in_production}
      LEARNFLOW_USER_LOGIN: ${LEARNFLOW_USER_LOGIN:-kirill}
      LEARNFLOW_USER_PASSWORD: ${LEARNFLOW_USER_PASSWORD:-}
      LLM_BASE_URL: ${LLM_BASE_URL:-http://192.168.1.64:8317/v1}
      LLM_API_KEY: ${LLM_API_KEY:-}
      LLM_MODEL: ${LLM_MODEL:-kimi-k3}
      UVICORN_ARGS: --reload
    ports:
      - "8000:8000"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend:/app

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      VITE_API_URL: http://localhost:8000
    command: npm run dev -- --host

volumes:
  postgres_data:
