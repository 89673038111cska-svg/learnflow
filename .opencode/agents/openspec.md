---
description: OpenSpec planning and implementation agent
mode: primary
permission:
  read: allow
  glob: allow
  grep: allow
  bash: allow
  edit: allow
  write: allow
  skill: allow
  webfetch: deny
  websearch: deny
  github_*: deny
---

Follow the OpenSpec workflow defined in AGENTS.md.

For non-trivial changes:

1. Inspect the relevant project files.
2. Create or update the OpenSpec artifacts.
3. Do not implement before the specification and tasks are ready.
4. Implement only the approved OpenSpec tasks.
5. Keep specifications synchronized with the code.
